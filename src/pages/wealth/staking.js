import * as React from "react";
import Layout from "../../components/Layout/Layout";

export default function Staking(){
    return (
        <Layout>
            <main className="pages">
                <section id="staking">Staking</section>
            </main>
        </Layout>
    )
}
